class_name PushableProperties extends Resource

@export var push_speed: float = 150.0          # How fast the object moves when pushed
@export var push_threshold: float = 0.5       # How long entity must push before movement starts
@export var move_distance: float = 16.0        # Distance to move (typically tile size)
